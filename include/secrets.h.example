#pragma once

// Copy this file to include/secrets.h and fill in your network.
// include/secrets.h is gitignored.
// These values are used on first boot. Wi-Fi (in access-point mode) and the
// login can be changed later on the settings page.

#define WIFI_SSID "your-ssid"
#define WIFI_PASSWORD "your-password"

// #define AUTH_USER "admin"
// #define AUTH_PASSWORD "esp-poweron"
